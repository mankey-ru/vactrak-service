# Public API base (no trailing slash). Browser + SSR $fetch.
NUXT_PUBLIC_API_BASE=http://localhost:3000

# Telegram bot username without @ (Login Widget). Configure domain in BotFather.
NUXT_PUBLIC_TELEGRAM_BOT_USERNAME=
