# Root docker-compose / local helpers. API secrets live in apps/api/.env
# (copy apps/api/.env.example). Web: apps/web/.env (see apps/web/.env.example).

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=vactrak_user
POSTGRES_PASSWORD=
POSTGRES_DB=vactrak_db
